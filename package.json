{
  "name": "zinc-subgraph",
  "license": "MIT",
  "author": "Sebastián Galiano <sgaliano@gmail.com>",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "create-local": "graph create --node http://localhost:8020/ sistemico/zinc",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ sistemico/zinc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sistemico/zinc",
    "remove-local": "graph remove --node http://localhost:8020/ sistemico/zinc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.13.3",
    "@graphprotocol/graph-ts": "0.13.0"
  }
}
